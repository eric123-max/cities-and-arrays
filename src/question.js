const cityDatabase = require("all-the-cities")
//const findcity = function(queryCity) {
  //  const qc = cityDB.find(function(it) {
   //     var keep = it.name === queryCity
     //   return keep
   // })
    //return city
//}
//console.log (cityDatabase[0])

const filtercity = function(featureC) {
    const fc = cityDB.filter(function(item) {
        var keep = it.featureCode === 'PPLA2'
        return keep
    })
}
console.log (cityDatabase)